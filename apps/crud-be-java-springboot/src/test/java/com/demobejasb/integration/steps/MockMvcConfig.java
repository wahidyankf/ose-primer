package com.demobejasb.integration.steps;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Retained as an empty placeholder to avoid breaking imports in any tooling cache.
 * MockMvc is no longer used by integration tests — step definitions call service
 * methods directly without HTTP.
 *
 * @deprecated No longer used. Integration tests call services directly.
 */
@Deprecated
@TestConfiguration
public class MockMvcConfig {}
