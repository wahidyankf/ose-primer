services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: crud_be_cjpd_test
      POSTGRES_USER: crud_be_cjpd
      POSTGRES_PASSWORD: crud_be_cjpd
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crud_be_cjpd -d crud_be_cjpd_test"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    ports:
      - "5432"
    tmpfs:
      - /var/lib/postgresql/data

  test-runner:
    build:
      context: .
      dockerfile: Dockerfile.integration
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: "jdbc:postgresql://postgres:5432/crud_be_cjpd_test"
      DB_USER: "crud_be_cjpd"
      DB_PASSWORD: "crud_be_cjpd"
      APP_JWT_SECRET: "integration-test-secret-key-at-least-32-characters"
    volumes:
      - ../../specs:/specs:ro
