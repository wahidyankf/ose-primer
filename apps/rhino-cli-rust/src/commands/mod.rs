// Command registry for rhino-cli.
//
// Phase 2 skeleton: intentionally empty. Each command module (agents_*, docs_*,
// doctor, env_*, git_*, governance_*, spec_coverage_*, test_coverage_*,
// workflows_*, …) ported from the Go CLI is declared here in later phases.
