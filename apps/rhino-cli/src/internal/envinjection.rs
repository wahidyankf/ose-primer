//! Backward-compatibility re-exports for the `envinjection` feature.
//!
//! The implementation lives in `crate::application::env::injection`.
//! This module keeps the `internal::*` access pattern consistent with siblings.
pub use crate::application::env::injection::*;
