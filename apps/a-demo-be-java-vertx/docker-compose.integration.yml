services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: a_demo_be_java_vertx
      POSTGRES_USER: a_demo_be_java_vertx
      POSTGRES_PASSWORD: a_demo_be_java_vertx
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U a_demo_be_java_vertx -d a_demo_be_java_vertx"]
      interval: 2s
      timeout: 5s
      retries: 10
    ports:
      - "5432"
    tmpfs:
      - /var/lib/postgresql/data

  test-runner:
    build:
      context: .
      dockerfile: Dockerfile.integration
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgresql://a_demo_be_java_vertx:a_demo_be_java_vertx@postgres:5432/a_demo_be_java_vertx"
    volumes:
      - ../../specs:/specs:ro
