# Local Development Environment for Demo Frontend (Dart/Flutter Web)
# Stack: Frontend + Backend (Go/Gin) + Database (PostgreSQL)

services:
  a-demo-be-db:
    image: postgres:17-alpine
    container_name: a-demo-fe-dfw-db
    environment:
      POSTGRES_DB: demo_be
      POSTGRES_USER: ${POSTGRES_USER:-a-demo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-a-demo}
    ports:
      - "5432:5432"
    volumes:
      - a-demo-fe-dfw-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-a-demo} -d demo_be"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - a-demo-fe-dfw-network

  demo-be:
    build:
      context: ../../../apps/a-demo-be-golang-gin
      dockerfile: Dockerfile
    container_name: a-demo-fe-dfw-be
    ports:
      - "8201:8201"
    depends_on:
      a-demo-be-db:
        condition: service_healthy
    environment:
      - PORT=8201
      - DATABASE_URL=postgresql://${POSTGRES_USER:-a-demo}:${POSTGRES_PASSWORD:-a-demo}@a-demo-be-db:5432/demo_be
      - APP_JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}
      - ENABLE_TEST_API=true
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      - a-demo-fe-dfw-network

  demo-fe:
    build:
      context: ../../../apps/a-demo-fe-dart-flutterweb
      dockerfile: Dockerfile
    container_name: a-demo-fe-dfw
    ports:
      - "3301:3301"
    depends_on:
      demo-be:
        condition: service_healthy
    environment:
      - BACKEND_URL=http://demo-be:8201
    restart: unless-stopped
    networks:
      - a-demo-fe-dfw-network

networks:
  a-demo-fe-dfw-network:
    driver: bridge

volumes:
  a-demo-fe-dfw-db-data:
