# Local Development Environment for Demo Frontend
# Stack: Frontend + Backend (Spring Boot) + Database (PostgreSQL)

services:
  demo-be-db:
    image: postgres:17-alpine
    container_name: demo-fe-dafl-db
    environment:
      POSTGRES_DB: demo_be
      POSTGRES_USER: ${POSTGRES_USER:-organiclever}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-organiclever}
    ports:
      - "5432:5432"
    volumes:
      - demo-fe-dafl-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-organiclever} -d demo_be"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - demo-fe-dafl-network

  demo-be:
    build:
      context: ../../../apps/demo-be-java-springboot
      dockerfile: Dockerfile
    container_name: demo-fe-dafl-be
    ports:
      - "8201:8201"
    depends_on:
      demo-be-db:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8201
      - SPRING_DATASOURCE_URL=jdbc:postgresql://demo-be-db:5432/demo_be
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER:-organiclever}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:-organiclever}
      - APP_JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}
      - ENABLE_TEST_API=true
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - demo-fe-dafl-network

  demo-fe:
    build:
      context: ../../../apps/demo-fe-dart-flutter
      dockerfile: Dockerfile
    container_name: demo-fe-dafl
    ports:
      - "3301:3301"
    depends_on:
      demo-be:
        condition: service_healthy
    environment:
      - BACKEND_URL=http://demo-be:8201
    restart: unless-stopped
    networks:
      - demo-fe-dafl-network

networks:
  demo-fe-dafl-network:
    driver: bridge

volumes:
  demo-fe-dafl-db-data:
