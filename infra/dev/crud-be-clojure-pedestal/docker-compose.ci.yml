# CI overlay — production-equivalent mode + test-only API + Next.js frontend
# Usage:
#   BE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d crud-be-clojure-pedestal
#   FE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d

services:
  crud-be-clojure-pedestal:
    environment:
      - PEDESTAL_ENV=production
      - ENABLE_TEST_API=true

  crud-fe:
    build:
      context: ../../..
      dockerfile: apps/crud-fe-ts-nextjs/Dockerfile
      args:
        BACKEND_URL: http://crud-be-clojure-pedestal:8201
    container_name: crud-be-cjpd-ci-fe
    ports:
      - "3301:3301"
    depends_on:
      crud-be-clojure-pedestal:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - crud-be-clojure-pedestal-network
