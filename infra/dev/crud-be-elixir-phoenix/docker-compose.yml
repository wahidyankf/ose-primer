# Local Development Environment for Demo Backend (EXPH — Elixir/Phoenix)
# Mutually exclusive with crud-be-java-springboot — both bind port 8201 and share the same DB.
# Do not run both stacks simultaneously.

services:
  crud-be-elixir-phoenix-db:
    image: postgres:17-alpine
    container_name: crud-be-elixir-phoenix-db
    environment:
      POSTGRES_DB: crud_be_elixir_phoenix
      POSTGRES_USER: ${POSTGRES_USER:-crud_be_elixir_phoenix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-crud_be_elixir_phoenix}
    ports:
      - "5432:5432"
    volumes:
      - crud-be-elixir-phoenix-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-crud_be_elixir_phoenix} -d crud_be_elixir_phoenix"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - crud-be-elixir-phoenix-network

  crud-be-elixir-phoenix:
    build:
      context: ../../..
      dockerfile: infra/dev/crud-be-elixir-phoenix/Dockerfile.be.dev
    container_name: crud-be-elixir-phoenix
    working_dir: /workspace

    # Mount app source and local Mix path dependencies.
    # Mix resolves path deps relative to /workspace:
    #   "../../libs/elixir-gherkin" → /libs/elixir-gherkin
    #   "../../libs/elixir-cabbage" → /libs/elixir-cabbage
    volumes:
      - ../../../apps/crud-be-elixir-phoenix:/workspace:rw
      - ../../../libs/elixir-gherkin:/libs/elixir-gherkin:ro
      - ../../../libs/elixir-cabbage:/libs/elixir-cabbage:ro
      - ../../../specs/apps/crud/be/gherkin:/specs/apps/crud/be/gherkin:ro

    ports:
      - "8201:8201"

    depends_on:
      crud-be-elixir-phoenix-db:
        condition: service_healthy

    environment:
      - MIX_ENV=dev
      - PORT=8201
      - DATABASE_URL=ecto://crud_be_elixir_phoenix:crud_be_elixir_phoenix@crud-be-elixir-phoenix-db:5432/crud_be_elixir_phoenix
      - APP_JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}

    # Run migrations before starting the server so fresh containers have the schema applied.
    command: sh -c "mix deps.get && mix ecto.migrate && mix phx.server"

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 600s

    restart: unless-stopped
    networks:
      - crud-be-elixir-phoenix-network

networks:
  crud-be-elixir-phoenix-network:
    driver: bridge
    name: crud-be-elixir-phoenix-network

volumes:
  crud-be-elixir-phoenix-db-data:
