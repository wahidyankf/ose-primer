# Local Development Environment for Demo Backend (KTKT — Kotlin/Ktor)
# Mutually exclusive with other demo-be backends — all bind port 8201.
# Do not run multiple backend stacks simultaneously.

services:
  crud-be-kotlin-ktor-db:
    image: postgres:17-alpine
    container_name: crud-be-kotlin-ktor-db
    environment:
      POSTGRES_DB: crud_be_kotlin_ktor
      POSTGRES_USER: ${POSTGRES_USER:-crud_be_kotlin_ktor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-crud_be_kotlin_ktor}
    ports:
      - "5432:5432"
    volumes:
      - crud-be-kotlin-ktor-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-crud_be_kotlin_ktor} -d crud_be_kotlin_ktor"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - crud-be-kotlin-ktor-network

  crud-be-kotlin-ktor:
    build:
      context: .
      dockerfile: Dockerfile.be.dev
    container_name: crud-be-kotlin-ktor
    working_dir: /workspace

    # Mount app source and shared Gherkin specs.
    volumes:
      - ../../../apps/crud-be-kotlin-ktor:/workspace:rw
      - ../../../specs/apps/crud/be/gherkin:/specs/apps/crud/be/gherkin:ro

    ports:
      - "8201:8201"

    depends_on:
      crud-be-kotlin-ktor-db:
        condition: service_healthy

    environment:
      - PORT=8201
      - DATABASE_URL=jdbc:postgresql://crud-be-kotlin-ktor-db:5432/crud_be_kotlin_ktor
      - DATABASE_USER=${POSTGRES_USER:-crud_be_kotlin_ktor}
      - DATABASE_PASSWORD=${POSTGRES_PASSWORD:-crud_be_kotlin_ktor}
      - JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}

    command: ./gradlew run

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

    restart: unless-stopped
    networks:
      - crud-be-kotlin-ktor-network

networks:
  crud-be-kotlin-ktor-network:
    driver: bridge
    name: crud-be-kotlin-ktor-network

volumes:
  crud-be-kotlin-ktor-db-data:
