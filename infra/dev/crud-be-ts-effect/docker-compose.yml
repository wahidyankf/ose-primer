# Local Development Environment for Demo Backend (TSEX — TypeScript/Effect)
# Mutually exclusive with other crud-be backends — all bind port 8201.
# Do not run multiple backend stacks simultaneously.

services:
  crud-be-ts-effect-db:
    image: postgres:17-alpine
    container_name: crud-be-ts-effect-db
    environment:
      POSTGRES_DB: crud_be_ts_effect
      POSTGRES_USER: ${POSTGRES_USER:-crud_be_ts_effect}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-crud_be_ts_effect}
    ports:
      - "5432:5432"
    volumes:
      - crud-be-ts-effect-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-crud_be_ts_effect} -d crud_be_ts_effect"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - crud-be-ts-effect-network

  crud-be-ts-effect:
    build:
      context: .
      dockerfile: Dockerfile.be.dev
    container_name: crud-be-ts-effect

    # Mount app source
    volumes:
      - ../../../apps/crud-be-ts-effect:/workspace:rw

    ports:
      - "8201:8201"

    depends_on:
      crud-be-ts-effect-db:
        condition: service_healthy

    environment:
      - PORT=8201
      - DATABASE_URL=postgresql://crud_be_ts_effect:crud_be_ts_effect@crud-be-ts-effect-db:5432/crud_be_ts_effect
      - APP_JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

    restart: unless-stopped
    networks:
      - crud-be-ts-effect-network

networks:
  crud-be-ts-effect-network:
    driver: bridge
    name: crud-be-ts-effect-network

volumes:
  crud-be-ts-effect-db-data:
