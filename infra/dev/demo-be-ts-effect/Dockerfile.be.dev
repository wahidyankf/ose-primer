# Development Docker Image for Demo Backend (TSEX — TypeScript/Effect)
# Uses Node.js 24 Alpine with npm for dependency management.

FROM node:24-alpine

# Install curl for healthcheck
RUN apk add --no-cache curl

WORKDIR /workspace

CMD ["sh", "-c", "npm install --no-package-lock && npx tsx src/main.ts"]
