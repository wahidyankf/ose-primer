# Local Development Environment for Demo Backend (FSGI — F#/Giraffe)
# Mutually exclusive with a-demo-be-java-springboot and a-demo-be-elixir-phoenix — all bind port 8201.
# Do not run multiple backend stacks simultaneously.

services:
  a-demo-be-fsharp-giraffe-db:
    image: postgres:17-alpine
    container_name: a-demo-be-fsharp-giraffe-db
    environment:
      POSTGRES_DB: a_demo_be_fsharp_giraffe
      POSTGRES_USER: ${POSTGRES_USER:-a_demo_be_fsharp_giraffe}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-a_demo_be_fsharp_giraffe}
    ports:
      - "5432:5432"
    volumes:
      - a-demo-be-fsharp-giraffe-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-a_demo_be_fsharp_giraffe} -d a_demo_be_fsharp_giraffe"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - a-demo-be-fsharp-giraffe-network

  a-demo-be-fsharp-giraffe:
    build:
      context: .
      dockerfile: Dockerfile.be.dev
    container_name: a-demo-be-fsharp-giraffe
    working_dir: /workspace

    # Mount app source and shared Gherkin specs.
    volumes:
      - ../../../apps/a-demo-be-fsharp-giraffe:/workspace:rw
      - ../../../specs/apps/a-demo/be/gherkin:/specs/apps/a-demo/be/gherkin:ro

    ports:
      - "8201:8201"

    depends_on:
      a-demo-be-fsharp-giraffe-db:
        condition: service_healthy

    environment:
      - DOTNET_ENVIRONMENT=Development
      - DATABASE_URL=Host=a-demo-be-fsharp-giraffe-db;Port=5432;Database=a_demo_be_fsharp_giraffe;Username=a_demo_be_fsharp_giraffe;Password=a_demo_be_fsharp_giraffe
      - APP_JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}

    command: dotnet run --project src/ADemoBeFsgi/ADemoBeFsgi.fsproj

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

    restart: unless-stopped
    networks:
      - a-demo-be-fsharp-giraffe-network

networks:
  a-demo-be-fsharp-giraffe-network:
    driver: bridge
    name: a-demo-be-fsharp-giraffe-network

volumes:
  a-demo-be-fsharp-giraffe-db-data:
