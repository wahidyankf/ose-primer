# CI overlay — production-equivalent mode + test-only API + Next.js frontend
# Usage:
#   BE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d crud-be-golang-gin
#   FE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d

services:
  crud-be-golang-gin:
    environment:
      - GIN_MODE=release
      - ENABLE_TEST_API=true

  crud-fe:
    build:
      context: ../../..
      dockerfile: apps/crud-fe-ts-nextjs/Dockerfile
      args:
        BACKEND_URL: http://crud-be-golang-gin:8201
    container_name: crud-be-gogi-ci-fe
    ports:
      - "3301:3301"
    depends_on:
      crud-be-golang-gin:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - crud-be-golang-gin-network
