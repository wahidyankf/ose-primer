# CI overlay — production-equivalent mode + test-only API + Next.js frontend
# Usage:
#   BE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d crud-be-java-springboot
#   FE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d

services:
  crud-be-java-springboot:
    environment:
      - MANAGEMENT_ENDPOINT_HEALTH_SHOWDETAILS=when-authorized
      - ENABLE_TEST_API=true

  crud-fe:
    build:
      context: ../../..
      dockerfile: apps/crud-fe-ts-nextjs/Dockerfile
      args:
        BACKEND_URL: http://crud-be-java-springboot:8201
    container_name: crud-be-jasb-ci-fe
    ports:
      - "3301:3301"
    depends_on:
      crud-be-java-springboot:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - crud-be-network
