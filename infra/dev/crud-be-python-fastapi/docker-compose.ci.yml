# CI overlay — production-equivalent mode + test-only API + Next.js frontend
# Usage:
#   BE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d crud-be-python-fastapi
#   FE E2E: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build -d

services:
  crud-be-python-fastapi:
    environment:
      - APP_ENV=production
      - ENABLE_TEST_API=true

  demo-fe:
    build:
      context: ../../..
      dockerfile: apps/crud-fe-ts-nextjs/Dockerfile
      args:
        BACKEND_URL: http://crud-be-python-fastapi:8201
    container_name: crud-be-pyfa-ci-fe
    ports:
      - "3301:3301"
    depends_on:
      crud-be-python-fastapi:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - crud-be-python-fastapi-network
