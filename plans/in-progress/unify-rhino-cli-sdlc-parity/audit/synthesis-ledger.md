# Synthesis Ledger — rhino-cli Canonical Union (Phase 1, ose-public)

> Records every **divergent-unit choice** made while synthesizing the canonical
> rhino-cli command surface + cucumber harness, per Decision 9 (most-evolved-wins
> tiebreak; deviations logged with a one-line reason). Scope of this ledger:
> the cucumber-harness + command-surface synthesis sub-portion of Phase 1
> (NOT the IaC-validator port, repo-config-validate, or LICENSE/docs items).

## Command-surface decisions

| Unit                                                                                            | Repos & variants                                                                                                                                                                     | Choice                                                                                                                                      | Reason                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test-coverage` CLI command                                                                     | infra = **live** (`TestCoverage::Validate` → `test_coverage_validate::run`); primer = **removed** (dead files + `test_coverage_validate_no_longer_parses` test); public = **absent** | Adopt **infra's live shape**: `Commands::TestCoverage(TestCoverageCommands::Validate)` wired to `test_coverage_validate::run`.              | Union superset requires the verb (delivery Phase-1 gate: `--help` lists `test-coverage`). Infra is the only repo where it is live and is depended on by per-project coverage gates; that is the most-evolved _shipped_ variant.                                                                                                          |
| `test_coverage_validate.rs`                                                                     | primer (domain::cliout) vs infra (internal::cliout)                                                                                                                                  | Port **primer's** file, retarget `internal::testcoverage`→`application::testcoverage` (public's module home).                               | Primer's uses `crate::domain::cliout::OutputFormat`, which exists in public; infra's `internal::cliout` does not exist in public.                                                                                                                                                                                                        |
| `commands/testcoverage.rs` (validate/diff/merge family)                                         | primer = present but **dead** (undeclared in `commands.rs`); infra = **unused**                                                                                                      | **Dropped** (not ported into the wired tree; the copied file was removed).                                                                  | Redundant command wrapper: neither primer nor infra wires it. `run_validate` duplicates `test_coverage_validate::run`; `run_diff`/`run_merge` wrap logic that is retained in `application/testcoverage/{diff,merge}.rs` (available, unit-tested), just not exposed as CLI subcommands — matching the live surface in both sibling repos. |
| `application/testcoverage/mod.rs`                                                               | primer's copy re-exports `crate::internal::testcoverage::*` (a forward to the _other_ module, not a real submodule tree)                                                             | Wrote a **real `mod.rs`** declaring `pub mod cobertura; … pub mod types;` for the 10 sibling files ported into `application/testcoverage/`. | Porting primer's forwarding stub would leave `application/testcoverage/*.rs` undeclared (dead files) and depend on `internal::testcoverage`, which is NOT ported (ledger: merged/redundant). The real mod.rs makes the ported siblings the actual implementation.                                                                        |
| `internal/testcoverage.rs` (public)                                                             | public had an **undeclared orphan** file re-exporting `application::testcoverage::*` (not listed in `internal.rs`)                                                                   | **Deleted** (dead-code cleanup).                                                                                                            | Undeclared, never compiled, superseded by the real `application::testcoverage` module now wired via `application/mod.rs`.                                                                                                                                                                                                                |
| `specs clean java-imports`, `specs scaffold dart`, `lang java null-safety-annotations validate` | public = **dormant stubs** ("dormant in ose-public"); primer = **real** (call `internal::contracts::*` / `internal::java::*`)                                                        | Replace public's stubs with **primer's real implementations** verbatim.                                                                     | Primer's are already-fixed/green and call the real ported logic; public's were no-op stubs that silently regressed the codegen pipelines. `ValidateNullSafetyArgs` gains an `annotation` field (clap picks it up; no cli.rs change needed).                                                                                              |

## Internal-tree decisions (ported per file-accounting dispositions)

| Unit                                                                                                                                                                                                                                                    | Choice                                                                                                                                                      | Reason                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `internal/contracts/{dart_scaffold,java_clean_imports,mod,types}.rs`                                                                                                                                                                                    | Ported from primer verbatim (+ doc-lint completeness for public's strict policy).                                                                           | `ported` disposition; backs the now-real contracts commands.                                                                          |
| `internal/java/{mod,reporter,scanner,types,validator}.rs`                                                                                                                                                                                               | Ported from primer verbatim (+ doc-lint completeness).                                                                                                      | `ported` disposition; backs the now-real `lang java` command.                                                                         |
| `application/testcoverage/{cobertura,detect,diff,exclude,go_coverage,jacoco,lcov,merge,reporter,types}.rs`                                                                                                                                              | Ported from primer verbatim; the two `#[cfg(test)]` refs to `crate::internal::testcoverage::types` retargeted to `crate::application::testcoverage::types`. | `ported` disposition; module now lives under `application/`.                                                                          |
| `internal/testcoverage/*`, `internal/cliout/*`, `internal/agents/*`, `internal/docs/*`, `internal/mermaid/*`, `internal/naming/reporter.rs`, `internal/speccoverage/*`, `internal/git/runner.rs`, `commands/git.rs`, `commands/specs_validate_links.rs` | **NOT ported.**                                                                                                                                             | `dropped`/`merged` dispositions — public's existing `application/*`/`domain/*`/`infrastructure/*` equivalents already supersede them. |

## Doc-lint reconciliation (public strict policy vs primer lenient)

| Unit                                                | Choice                                                                                                 | Reason                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ported `internal/{contracts,java}` `.rs`            | Added missing `# Errors` sections + private-item doc comments; backtick-quoted `OpenAPI`.              | Public's `Cargo.toml` denies `missing_errors_doc` / `missing_docs_in_private_items` / `doc_markdown`; primer wrote them under `allow`. Fixed the files, not the policy.                                                                                                                               |
| 9 cucumber test files (`tests/*.rs`, harness=false) | Added per-file `#![allow(clippy::missing_docs_in_private_items)]` + `#![allow(clippy::doc_markdown)]`. | `harness=false` test binaries get these doc lints enforced (harness=true tests are toolchain-exempt — e.g. `golden_master.rs`). Step-def scaffolding is self-documenting via the `#[given]/#[when]/#[then]` strings; matches the existing `#![allow(clippy::panic)]` precedent in `golden_master.rs`. |

## Cucumber-suite reconciliations to public's canonical behavior

| Suite                                         | Divergence found                                                                                                                                                                                                                                                                                                     | Fix                                                                                                                                                                                                                                                                                   |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `doctor.rs`                                   | Public's doctor checks **13** tools (git, volta, node, npm, rust, cargo-llvm-cov, dotnet, docker, jq, shellcheck, hadolint, actionlint, playwright); primer's suite stubbed a 19-tool polyglot set and asserted `tools.len()==19`. Empty-PATH "missing tool" scenario broke public's `git rev-parse` root discovery. | Added `shellcheck`/`hadolint`/`actionlint` stubs; changed `tools.len()` assertion to 13; changed the "missing tool" setup to keep git present and omit one probed tool (dropped the now-unused `empty_path` field). Public doctor is canonical (most-evolved for public's toolchain). |
| `docs.rs` (mermaid `--quiet`)                 | Public's `docs validate-mermaid` hard-coded `format_text(false,false)`, dropping the global `--verbose`/`--quiet` (its reporter already supports + unit-tests quiet-suppression).                                                                                                                                    | **Public bug fix (Iron Rule 3):** threaded `verbose`/`quiet` from the root `Cli` through `dispatch`→`dispatch_md`→`md_validate_mermaid::run` (and the `md audit` caller). No default-behavior change (both default false).                                                            |
| `env.rs` (backup rejects inside-repo `--dir`) | Public's `env backup` used `std::fs::canonicalize(...).unwrap_or(raw)`, which fails for a not-yet-existing `--dir` and leaves a non-canonical path; on macOS `git rev-parse` returns `/private/var/...` while `--dir` is `/var/...`, so `is_inside_repo` missed it. Primer carries a fix public lacks.               | **Bugfix-wins tiebreak:** ported primer's `canonicalize_best_effort` into `application/env/backup.rs` and used it in `commands/env_backup.rs` for both the default and explicit `--dir` branches.                                                                                     |

## Test/harness updates required by the command-surface change

| Unit                                                         | Choice                                                                                                                                                                                   | Reason                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cli.rs` unit test `test_coverage_validate_no_longer_parses` | Renamed to `test_coverage_validate_still_parses` and inverted to assert the command **does** parse.                                                                                      | The union re-adds the `test-coverage` verb; the old test encoded the removed-command state (mirror of infra's `test_coverage_validate_still_parses`).                                                                                                                                                                                                               |
| `tests/golden_master.rs` + `tests/golden-master/**`          | Adopted primer's harness (`{{TMPDIR}}` sentinel + `--no-color`); added `--dir {{TMPDIR}}` to the 3 leaf-command manifest entries; regenerated the full corpus from the canonical binary. | Adding `test-coverage` changed top-level `--help` (captured by 67 corpus entries via the global `--help` handler), and the 3 formerly-dormant leaf commands now emit real output (one, `specs scaffold dart`, writes files — must target a scratch dir, not the crate cwd). This is the golden-master regeneration that the command-surface synthesis necessitates. |
