PaginationEnvelope:
  type: object
  description: Reusable pagination metadata fields
  required:
    - totalElements
    - totalPages
    - page
    - size
  properties:
    totalElements:
      type: integer
    totalPages:
      type: integer
    page:
      type: integer
    size:
      type: integer
