#!/usr/bin/env sh
set -e

# Quality gates: typecheck, lint, test:quick for affected projects + markdown linting
# Pre-commit only runs rhino-cli checks (formatting, link validation, config sync)
# Pre-push runs the full quality gate
npx nx affected -t typecheck lint test:quick --parallel=8
npm run lint:md
